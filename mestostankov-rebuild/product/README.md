## product/

This is the deployable website (frontend + backend + database schema).

Planned high-level layout:

- `frontend/`: Next.js app (public website)
- `backend/`: optional separate API (only if Next.js API routes aren’t enough)
- `db/`: schema/migrations/seed
- `resources/`: imported media and other artifacts

